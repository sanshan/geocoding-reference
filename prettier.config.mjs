/** @type {import("prettier").Config} */
const config = {
    printWidth: 100,
    tabWidth: 4,
    useTabs: false,

    semi: true,
    singleQuote: true,
    trailingComma: 'all',

    bracketSpacing: true,
    arrowParens: 'always',
    endOfLine: 'lf',
};

export default config;